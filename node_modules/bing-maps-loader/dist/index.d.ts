declare const getApiUrl: (apiKey: string) => string;
/**
 * Fire off the request to load the API
 * @param apiKey
 * @param modulesToLoad Use the Module names list to populate
 */
declare const initialize: (apiKey: string, ...modulesToLoad: string[]) => Promise<void> | undefined;
/**
 * For SSR implementations, the script url + api key will already be in the HTML head.
 * @param modulesToLoad Use the Module names list to populate
 */
declare const initializeSSR: (...modulesToLoad: string[]) => void;
/** Sets up Promises for the CALLBACK
 */
declare const whenLoaded: Promise<void>;
declare const loadModule: (moduleName: string) => Promise<void>;
declare const moduleNames: {
    AutoSuggest: string;
    Clustering: string;
    Contour: string;
    DataBinning: string;
    Directions: string;
    DrawingTools: string;
    GeoJson: string;
    GeoXml: string;
    HeatMap: string;
    Search: string;
    SpatialDataService: string;
    SpatialMath: string;
    Traffic: string;
    WellKnownText: string;
    VenueMaps: string;
};
export { initialize, whenLoaded, initializeSSR, getApiUrl, loadModule, moduleNames, };
